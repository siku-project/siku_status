TranslationConfig = {
  --- Language
  ---
  --- The language used for every status translation (system strings and
  --- the threshold notifications).
  ---
  --- Available: 'en', 'fr'
  ---
  --- Default: 'fr'
  language = 'fr',
}
